#!/usr/bin/node
const args = process.argv;
if (isNaN(Number(args[2]))) {
    console.log('Missing size');
} else {
    for (i = 0; i < num; i++) {
    let row = ''
      for (j = 0; j < num; j++) {
      row+= 'X';
      }
console.log(row+'')
    }
}
